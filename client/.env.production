# Baked into the bundle at build time by Vite. Replace with the Cloud Run URL printed by
# `gcloud run deploy`, then rebuild — changing this file has no effect on an existing build.
VITE_API_URL=https://crossval-api-863312702719.us-central1.run.app/api/v1
